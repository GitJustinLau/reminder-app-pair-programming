import React from 'react';
import Timer from '../../components/Timer/Timer';

const HomePage = () => {
    return (
        <div>
            <Timer />
        </div>
    );
};

export default HomePage;
